# Inicializamos el contador
contador = 0

# Definimos la condición de inicio del bucle
while contador < 5:
    print(f"El contador es: {contador}")
    contador += 1  # Incrementamos el contador en cada iteración

print("Bucle completado.")
